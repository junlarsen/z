findSecretById:
SELECT * FROM secrets WHERE id = ?;

deleteSecretById:
DELETE FROM secrets WHERE id = ?;

updateSecretById:
UPDATE secrets SET secret = ?, expires_at = ?, remaining_views = ? WHERE id = ? RETURNING *;

createSecret:
INSERT INTO secrets (secret, expires_at, remaining_views) VALUES (?, ?, ?) RETURNING *;
